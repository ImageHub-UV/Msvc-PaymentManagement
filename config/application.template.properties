# spring.application.name=payment_management

# Set the credentials of your database here
# This should be used only for credentials and other local-only config.
# spring.datasource.url =
# spring.datasource.username =
# spring.datasource.password =

# spring.sql.init.mode=always

# Encryption properties
# encryption.secretKey =
# encryption.algorithm =

# Finally, copy and paste this file as "application.properties" in the same directory

